<!-- Di halaman EJS Anda (verify_otp.ejs) -->
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verifikasi OTP</title>
  <link rel="stylesheet" href="/css/tailwind.css"> <!-- Sertakan stylesheet Tailwind CSS -->
</head>

<body class="bg-gray-100 p-4 flex items-center justify-center h-screen">
  <div class="max-w-md bg-white p-6 rounded-md shadow-md">
    <h1 class="text-2xl font-semibold mb-4">Verifikasi OTP</h1>

    <p class="mb-4">Silakan masukkan OTP yang telah kami kirimkan ke email Anda.</p>

    <form action="/otp" method="POST">
      <input type="hidden" name="idUser" value="<%= idUser.toString() %>">
      <div class="mb-4">
        <label for="otp" class="block text-sm font-medium text-gray-700">OTP</label>
        <input type="text" id="otp" name="otp"
          class="w-full px-3 py-2 border rounded focus:outline-none focus:ring focus:border-blue-300"
          placeholder="Masukkan OTP" required>
      </div>

      <button type="submit"
        class="bg-blue-500 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-full w-full">
        Verifikasi OTP
      </button>
    </form>
  </div>
</body>

</html>